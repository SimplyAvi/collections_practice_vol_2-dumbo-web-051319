# your code goes here

def begins_with_r(array)
  def comp_value = array.find_all {|i| array[0] == "r"}
  end
end
if comp_value.length = array.length
    return "they all begin with the letter r"
  else
    return "it doesnt all beging with r"
  end
end